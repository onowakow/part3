// As with program wrote in part3-notes, this line is akin to import express from 'express'
const express = require('express')
// Call express function and load (I don't believe 'load' really captures what happens here, but IDK what exactly happens) into app.
const app = express()